Only search packages from TUR repository when auto-updating.
--- a/scripts/bin/update-packages
+++ b/scripts/bin/update-packages
@@ -415,6 +417,11 @@ _should_update() {
 		termux_error_exit "directory '${pkg_dir}' is not a package."
 	fi
 
+	if [[ "$(basename "$(realpath "$(dirname "${pkg_dir}")")")" != *"tur"* ]]; then
+		echo "INFO: Skipping '$(basename "${pkg_dir}")', it is not a TUR package."
+		return 1
+	fi
+
 	# `[[ "$(<"$file")" =~ regex ]]` is bash builtin solution for `grep -q` without spawning extra process
 	# it is needed to pre-fetch and filter packages as fast as possible (it is 6.5 times faster than `grep`)
 	case "${PACKAGES_OPT_OUT:-}" in
@@ -490,7 +490,7 @@ fi
 
 if [[ "$1" == "@all" ]]; then
 	declare -a PACKAGE_LIST=()
-	for channel in $(jq --raw-output 'del(.pkg_format) | keys | .[]' "${TERMUX_SCRIPTDIR}/repo.json"); do
+	for channel in "tur" "tur-on-device" "tur-multilib" "tur-hacking"; do
 		for pkg_dir in "${channel}"/*; do
 			PACKAGE_LIST+=("$pkg_dir")
 		done
@@ -503,7 +503,7 @@ declare -a PACKAGE_DIRS_LIST=()
 for pkg in "$@"; do
 	pkg_dir="${pkg}"
 	if [[ ! -d "${pkg}" ]]; then # If only package name is given, try to find its directory.
-		for channel in $(jq --raw-output 'del(.pkg_format) | keys | .[]' "${TERMUX_SCRIPTDIR}/repo.json"); do
+		for channel in "tur" "tur-on-device" "tur-multilib" "tur-hacking"; do
 			if [[ -d "${channel}/${pkg}" ]]; then
 				pkg_dir="${channel}/${pkg}"
 				break
Sleep 1s after update in order to not reach GitHub rate limit.
--- a/scripts/bin/update-packages
+++ b/scripts/bin/update-packages
@@ -130,6 +130,7 @@ _update() {
 	exec {IPC_FIFO_FD}<>"$IPC_FIFO"
 	termux_pkg_auto_update 3>& "${IPC_FIFO_FD}"
 	exec {IPC_FIFO_FD}>&-
+	sleep 1s
 }
 
 declare -A _LATEST_TAGS=()
Do not run check-updated step, which will make it easy to reach GitHub rate limit.
--- a/scripts/bin/update-packages
+++ b/scripts/bin/update-packages
@@ -316,6 +317,7 @@ _fetch_and_cache_tags() {
 }
 
 _check_updated() {
+	return 1
 	local latest_tag="${_LATEST_TAGS[${1##*/}]:-}"
 
 	if [[ -z "${latest_tag}" ]]; then
--- a/scripts/bin/build-package-dry-run-simulation.sh
+++ b/scripts/bin/build-package-dry-run-simulation.sh
@@ -12,6 +12,10 @@ TERMUX_PACKAGES_DIRECTORIES="
 packages
 root-packages
 x11-packages
+tur
+tur-on-device
+tur-multilib
+tur-hacking
 "
 
 # Please keep synchronized with the logic of lines 468-547 of 'build-package.sh'.
