--- Boxedwine.orig/lib/tiny-process/process_unix.cpp	2023-07-02 02:53:49
+++ Boxedwine/lib/tiny-process/process_unix.cpp	2023-08-15 01:35:28
@@ -33,7 +33,9 @@
   char cspath[PATH_MAX + 1] = {};
   if(!path) {
     // If env variable is not set, use static path string.
-    confstr(_CS_PATH, cspath, sizeof(cspath));
+    //confstr(_CS_PATH, cspath, sizeof(cspath));
+    strncpy(cspath, "@TERMUX_PREFIX@/bin:@TERMUX_PREFIX@/sbin", sizeof(cspath));
+    cspath[sizeof(cspath) - 1] = '\0';
     path = cspath;
   }
 
