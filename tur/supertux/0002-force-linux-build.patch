diff -ur a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt	2025-12-29 11:50:50.000000000 +0100
+++ b/CMakeLists.txt	2025-12-30 12:21:08.150017697 +0100
@@ -40,7 +40,7 @@
 ## Project name to use as command prefix.
 
 set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${CMAKE_CURRENT_SOURCE_DIR}/mk/cmake)
-if(ANDROID)
+if(0)
   include(SuperTux/Android)
 endif()
 
@@ -98,7 +98,7 @@
   endif()
 endif()
 
-if(EMSCRIPTEN OR ANDROID)
+if(EMSCRIPTEN OR 0)
   option(REMOVE_QUIT_BUTTON "Remove the option to quit the game (useful on mobile devices)" ON)
 else()
   option(REMOVE_QUIT_BUTTON "Remove the option to quit the game (useful on mobile devices)" OFF)
@@ -112,14 +112,14 @@
 option(SUPERTUX_LTO "Use link-time optimizations (Takes more RAM at compilation, gives smaller and faster executables)" OFF)
 
 # Mobile builds
-if(ANDROID)
+if(0)
   option(HIDE_NONMOBILE_OPTIONS "Hide options that are impractical on mobile devices (e. g. changing screen resolution)" ON)
 else()
   option(HIDE_NONMOBILE_OPTIONS "Hide options that are impractical on mobile devices (e. g. changing screen resolution)" OFF)
 endif()
 
 ## Determine whether static linking for SimpleSquirrel should be defaulted to
-if(MSVC OR ANDROID)
+if(MSVC OR 0)
   option(USE_STATIC_SIMPLESQUIRREL "Statically link the SimpleSquirrel library" ON)
 else()
   option(USE_STATIC_SIMPLESQUIRREL "Statically link the SimpleSquirrel library" OFF)
@@ -278,7 +278,7 @@
 include(SuperTux/CompileAmalgation)
 
 ## Add target for supertux binary
-if(ANDROID)
+if(0)
   add_library(supertux2 SHARED ${CMAKE_BINARY_DIR}/version.h ${SUPERTUX_SOURCES_C} ${SUPERTUX_SOURCES_CXX} ${SUPERTUX_SOURCES_HXX} src/main.cpp)
 else()
   add_executable(supertux2 ${SUPERTUX_WIN32_OPTION} ${CMAKE_BINARY_DIR}/version.h ${SUPERTUX_SOURCES_C} ${SUPERTUX_SOURCES_CXX} ${SUPERTUX_SOURCES_HXX} ${SUPERTUX_RESOURCES} src/main.cpp)
@@ -433,7 +433,7 @@
   endif()
 endif()
 
-if(ANDROID)
+if(0)
   target_link_libraries(supertux2 PUBLIC log)
   target_compile_definitions(supertux2 PUBLIC -D__ANDROID__ -DANDROID)
 endif()
@@ -445,7 +445,7 @@
 configure_file(config.h.in ${CMAKE_BINARY_DIR}/config.h)
 
 # Make android data.zip
-if(ANDROID)
+if(0)
   set(ANDROID_PROJECT_DIR "${PROJECT_SOURCE_DIR}/mk/android/" CACHE PATH
           "Path to the root of the root of the android project")
   if(NOT EXISTS "${ANDROID_PROJECT_DIR}/app/src/main/assets/data.zip")
diff -ur a/src/supertux/main.cpp b/src/supertux/main.cpp
--- a/src/supertux/main.cpp	2025-12-29 11:50:50.000000000 +0100
+++ b/src/supertux/main.cpp	2025-12-30 12:21:22.017788541 +0100
@@ -199,7 +199,7 @@
 void PhysfsSubsystem::find_mount_datadir()
 {
 #ifndef __EMSCRIPTEN__
-#ifdef __ANDROID__
+#if 0
     if (!setup_android_datadir())
     {
       log_warning << "Couldn't setup android assets: " << PHYSFS_getErrorByCode(PHYSFS_getLastErrorCode()) << std::endl;
@@ -489,7 +489,7 @@
     throw std::runtime_error(msg.str());
   }
 
-#ifdef __ANDROID__
+#if 0
   g_config->mobile_controls = SDL_GetNumTouchDevices() > 0;
 #endif
 
@@ -772,11 +772,7 @@
       return EXIT_FAILURE;
     }
 
-#ifdef __ANDROID__
-    m_physfs_subsystem.reset(new PhysfsSubsystem(nullptr, args.datadir, SDL_AndroidGetExternalStoragePath()));
-#else
     m_physfs_subsystem.reset(new PhysfsSubsystem(nullptr, args.datadir, args.userdir));
-#endif
     m_physfs_subsystem->print_search_path();
 
     s_timelog.log("config");
@@ -822,7 +818,7 @@
 
   g_dictionary_manager.reset();
 
-#ifdef __ANDROID__
+#if 0
   // SDL2 keeps shared libraries loaded after the app is closed,
   // when we launch the app again the static initializers will run twice and crash the app.
   // So we just need to terminate the app process 'gracefully', without running destructors or atexit() functions.
diff -ur a/src/util/log.cpp b/src/util/log.cpp
--- a/src/util/log.cpp	2025-12-29 11:50:50.000000000 +0100
+++ b/src/util/log.cpp	2025-12-30 12:21:28.561680390 +0100
@@ -18,7 +18,7 @@
 #include "util/log.hpp"
 
 #include <iostream>
-#ifdef __ANDROID__
+#if 0
 #include <android/log.h>
 #endif
 
@@ -27,7 +27,7 @@
 #include "supertux/gameconfig.hpp"
 #include "supertux/globals.hpp"
 
-#ifdef __ANDROID__
+#if 0
 // To print only SuperTux logs in Android logcat output, use command:
 // adb logcat -s 'SuperTux:V' 'SDL:V' 'DEBUG:V'
 class _android_debugbuf: public std::streambuf
@@ -105,7 +105,7 @@
   if (ConsoleBuffer::current() && use_console_buffer)
     return (ConsoleBuffer::output);
   else
-#ifdef __ANDROID__
+#if 0
     return android_logcat;
 #else
     return (std::cerr);
